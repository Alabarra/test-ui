export interface CategoryItem {
    id: string;
    name: string;
  }

export interface ProductItem{
    id:string;
    title: string;
    picture:string;
    offerPrice:number;
    normalPrice:number;
    description:string;
    showIcon:boolean;
}